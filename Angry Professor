def angryProfessor(k, a):
    list = [i for i in a if i <= 0]
    return "YES" if len(list) < k else "NO"



def angryProfessor(k, a):
    p = 0
    for i in a :
        if i <= 0  :
            p += 1
    if p >= k :
        return "NO"
    return "YES"
